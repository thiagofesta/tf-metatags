language: node_js
node_js:
  - "0.10"

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - npm install -g grunt-cli

script:
  - grunt test

after_script:
  - NODE_ENV=cat ./test/coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js
